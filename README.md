# Ansible-control-server-2-managed-EC2-servers
This assignment is asking you to build an Ansible control server + 2 managed EC2 servers in the same AWS VPC, configure SSH connectivity, and run an Ansible ping playbook.
